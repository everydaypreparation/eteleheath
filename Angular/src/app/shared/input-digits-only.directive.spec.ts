import { InputDigitsOnlyDirective } from './input-digits-only.directive';

describe('InputDigitsOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new InputDigitsOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
